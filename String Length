def string_length(s):
    count = 0
    for _ in s:
        count += 1
    return count

user_input = input("Enter a string: ")

length = string_length(user_input)

print("Length of the string:", length)
